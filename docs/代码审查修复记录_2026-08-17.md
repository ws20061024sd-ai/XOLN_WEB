# 代码审查修复记录（2026-08-17）

> 本次审查为只读审查后的修复。按用户要求，评论/留言的默认审核策略**暂不修改**。

## 已修复

### 后端

| 文件 | 修复内容 |
|------|---------|
| `server/src/routes/admin.ts` | 修复子应用鉴权中间件路径（`/*` 而非 `/api/admin/*`）；`ADMIN_KEY` 改为必填且至少 12 位 |
| `server/src/load-env.ts` | 新增，开发环境自动加载 `server/.env` |
| `server/src/db/schema.ts` | 修复并发初始化竞态；写盘改为临时文件 + rename 原子替换 |
| `server/src/routes/comments.ts` | 增加字段类型、slug/section 长度校验 |
| `server/src/routes/contact.ts` | 增加 name/email 长度校验和邮箱格式校验 |
| `server/src/routes/community.ts` | 增加字段类型和 tags 长度校验 |
| `server/src/routes/guestbook.ts` | 增加字段类型校验 |
| `server/src/routes/stats.ts` | 增加 path 类型和长度校验 |
| `server/Dockerfile` | 改为多阶段构建，容器内 `npm ci` + `tsc`，不再依赖本地 dist |
| `server/docker-compose.yml` | 端口只绑定 `127.0.0.1`；强制要求 `ADMIN_KEY` |
| `server/.env.example` | 新增模板；`.env` 已在 .gitignore 中排除 |

### 前端

| 文件 | 修复内容 |
|------|---------|
| `src/lib/api.ts` | 统一导出 `API_BASE`；所有请求函数增加网络异常兜底；社区详情 404 返回 null |
| `src/app/admin/AdminPanel.tsx` | API 地址改为从 `lib/api.ts` 引入 |
| `src/app/guestbook/GuestbookClient.tsx` | API 地址统一；提交/加载增加错误处理 |
| `src/components/CommentSection.tsx` | 提交增加 try/catch/finally，避免按钮卡死 |
| `src/components/ContactForm.tsx` | 同上 |
| `src/app/community/page.tsx` | 同上 |
| `src/app/works/[...path]/page.tsx` | 评论 slug 改用完整路径，避免同名文件评论串台 |

### 部署脚本

| 文件 | 修复内容 |
|------|---------|
| `upload.js` | 部分上传失败时不再把失败文件写入 manifest；上传失败自动重试；manifest 失败会报错；删除旧文件后同步 manifest |
| `upload-quant.js` | 新增，量化项目 output 一键上传到 COS `works/quant/` |
| `package.json` | 新增 `npm run upload:quant` |

### 文档

- `site/README.md`：替换模板为真实项目说明
- `docs/后端架构方案.md`：同步实际技术栈、审核策略、限流、Dockerfile、密钥管理和安全注意事项
- `docs/开发审查清单.md`：更新服务端部署流程、密钥卡片、量化仪表盘上传步骤
- `docs/当前状态与待办.md`：记录本次修复
- `site/content/changelog/12-security-fix.md`：站点更新日志

## 仍保留（用户决定暂不修改）

- 评论、留言默认 `approved=1`，提交后直接公开
- 日语学习舱 Supabase 共享单用户数据的结构未改
- 管理列表分页、搜索页打包全站正文等 P2/P3 优化未做

## 部署注意

1. 在服务器 `server/.env` 配置 `ADMIN_KEY`（与本地一致或单独生成）。
2. 上传 `server/` 源码后执行 `docker compose up -d --build`，不再 scp dist。
3. 前端重新 `npm run build` 并 `node upload.js`。
4. 量化仪表盘重新生成后执行 `npm run upload:quant`。
