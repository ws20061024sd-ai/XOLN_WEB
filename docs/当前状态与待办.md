# 当前状态与待办

> 2026-05-30。共创栏目和作品多级文件夹分类已上线，更新栏目 9 篇，38 个静态页面。

---

## 一、已解决：作品栏目中文路径 dev 模式报错

**问题**：`content/works/` 下用中文命名子目录（"文稿"、"设计"），`npm run dev` 访问嵌套页面时报 500：
> Page "/works/[...path]/page" is missing param "/works/[...path]" in "generateStaticParams()"

**原因**：Next.js 16.2.6 (Turbopack) dev 模式下，`generateStaticParams` 对含中文的 catch-all 路由参数匹配失败。英文路径正常。`npm run build` 和生产环境无此问题。

**方案**：子目录统一用英文命名。URL 更干净（`/works/design/` vs `/works/%E8%AE%BE%E8%AE%A1/`），显示可通过 frontmatter `title` 字段保持中文。

**实际执行**：`文稿` → `writing`，`设计` → `design`。dev 模式全线 200。

---

## 二、最近完成

- **作品栏目文件夹式分类**：`works/[...path]` catch-all 路由，子目录 = 系列，支持任意深度
- **共创栏目上线**：读者 Markdown 投稿 + 审核 + 评论，首页卡片 + 导航入口
- **三观内容架构完善**：世界观/人生观/价值观 + 爱情观

---

## 二、运行状态

| 组件 | 状态 |
|------|------|
| 本地开发 | ✅ localhost:3099 |
| 后端 API | ✅ api.xolnxoln.cn |
| 网站 xolnxoln.cn | ✅ |
| 网站 www.xolnxoln.cn | ✅ |
| 留言板 | ✅ /guestbook |
| 共创栏目 | ✅ /community（含评论） |
| ICP 备案 | ✅ 湘ICP备2026019191号 |
| 公安备案 | ✅ 已完成 |
| SSH/SCP | ❌ 网络层阻断（用 WebShell 替代） |

---

## 三、备案情况

| 事项 | 状态 |
|------|------|
| 管局 ICP 备案 | ✅ 湘ICP备2026019191号 |
| ICP 备案号悬挂 | ✅ 页脚已添加 |
| 公安联网备案 | ✅ 已完成 |

---

## 四、本地开发注意事项

- 启动命令：`npm run dev -- --port 3099`（不是 `npm run dev`，默认是 3000）
- 打不开时：[docs/开发审查清单.md](docs/开发审查清单.md) 第五节速查
