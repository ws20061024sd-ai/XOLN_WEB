# 当前状态与待办

> 2026-05-19 更新。备案通过，网站可通过 HTTP 访问，HTTPS 已部署。

---

## 一、运行状态

| 组件 | 状态 | 说明 |
|------|------|------|
| 本地开发 | ✅ | `localhost:3099` |
| 后端 API | ✅ | `http://api.xolnxoln.cn`，12 个接口 |
| COS 桶 | ✅ | `xolnxoln-1431302682`，295 个文件，Content-Type 正确 |
| 静态网站托管 | ✅ | `cos-website` 端点已开启 |
| 域名 HTTPS | ✅ | Let's Encrypt 证书已部署，自动续期 |
| 网站访问 | ⚠️ | HTTP 正常，HTTPS 浏览器可能有 DNS 缓存导致打不开 |
| CDN | ⏳ | 已添加域名但备案未同步，等腾讯云自动刷新 |
| ICP 备案 | ✅ | 湘ICP备2026019191号，已悬挂页脚 |
| 公安备案 | ⏳ | 进行中，数据码 `e49cb0a4a4831469254086cd4997e239` |

---

## 二、当前架构

```
用户 → xolnxoln.cn → DNS (A: 193.112.220.113)
       → Lighthouse Nginx
           → HTTPS: 443 (SSL)
           → HTTP: 80 (跳 HTTPS)
       → 反代到 COS cos-website 端点
       → 返回网页

用户 → api.xolnxoln.cn → DNS (A: 193.112.220.113)
       → Lighthouse Nginx
       → 反代到 Docker 容器 (3001)
       → Hono API
```

---

## 三、待办事项

### 紧急

- CDN：等待腾讯云 CDN 系统同步备案数据（1-2 天），同步后将 DNS 改为 CDN CNAME 地址
- 浏览器 HTTPS：清 DNS 缓存或用手机 4G 测试 `https://xolnxoln.cn`

### 常规

- 继续填充 `content/` 下的 `.md` 内容
- 公安联网备案：登录 https://beian.mps.gov.cn 提交
- 以后上传 COS：`cd site && npm run build` 然后执行上传脚本（设好 Content-Type）

---

## 四、不要再做的事

- ❌ DNS 直接 CNAME 到 COS（不管加不加 -website，自定义域名 COS 都不认）
- ❌ 反复改 DNS 记录
- ✅ 等 CDN 备案同步后一条路：CDN → COS
