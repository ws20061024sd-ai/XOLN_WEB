# 当前状态与待办

> 2026-08-17。作品栏目 201 篇文章，2177 个静态文件，搜索已优化为递归+全文。

---

## 一、COS 上传耗时分析与增量方案

### 现状

每次 `npm run build` 生成 2177 个文件，上传 COS 耗时 4-7 分钟。但多数时候只改了几篇 `.md` 内容。

### 已完成：增量上传脚本

`upload.js` v2 已实现 SHA256 哈希对比增量上传：

1. 遍历 `out/` 所有文件，计算 SHA256 哈希
2. 从 COS 下载上次的 `upload-manifest.json` 对照
3. 哈希相同的跳过，不同/新增的上传，旧有本地无的从 COS 删除
4. 上传完成后更新 manifest 并存回 COS

没改动内容时重跑上传 → 2177 全跳过，< 1 秒完成。

### 实际问题：不在上传，在构建

增量上传本身是成功的。但实测发现：**改了一篇 .md 内容后 `npm run build`，输出目录里 2177 个文件有 2150 个变了。**

原因：Next.js 的 `_next/static/chunks/` 下的 JS/CSS 文件名包含构建哈希，每次 `npm run build` 都生成不同的哈希值，即使代码和内容都没变。这 2000+ 个 JS/CSS 占了上传时间的 95% 以上。

流程图：

```
改一篇 .md
  → npm run build（全量，30s）
  → out/ 里 2177 个文件几乎全变了 ← 瓶颈在这
  → upload.js 增量对比（秒级）
  → 还是要传 2000+ 个文件（4-7 分钟）← 只是因为构建哈希变了
```

### 探索过的方案（不可行）

**轻量 HTML 替换**：尝试只替换改了内容的 HTML 页面，不动 JS。但 Next.js 页面内容同时嵌在 HTML 和 `self.__next_f.push(...)` 水合脚本里，两者必须一致，否则浏览器报错或评论功能失效。结论：不能只换 HTML。

### 可行方案：上传并行化

当前 `upload.js` 文件逐个串行上传，2177 个文件 = 4-7 分钟。改成 10 个并发上传，时间缩短到 ~30 秒。配合已实现的增量对比（不改动 0 上传），实际体验大幅提升。

**实施**：`upload.js` 加并发控制，每次 10 个文件同时上传。串行 → 并行，零风险，不改构建流程。

### 量化仪表盘上传

`/works/quant` 页面通过 iframe 加载 `/works/quant/index.html`，但该文件不在 Next.js 构建产物中。量化项目生成 `web/output/` 后，需要单独运行：

```bash
cd site && npm run upload:quant
```

脚本会把 `量化/web/output/` 全量上传到 COS 的 `works/quant/` 前缀。可用 `QUANT_OUTPUT_DIR` 覆盖默认目录。

---

## 二、待办：搜索结果展示正文匹配片段

### 问题

搜索已支持正文内容匹配，但搜索结果只显示**标题 + 描述 + 标签**。用户搜到一个关键词，看到的却是：

```
世界观
我如何看待这个世界。
[世界观] [思考]
```

无法判断这篇文章里到底写了什么相关内容，也不知道关键词出现在文章的哪个位置。得点进每篇文章慢慢找。

### 方案

搜索结果卡片中新增**正文匹配片段（snippet）**：

**效果示例**：

搜"自然选择"，结果变成：

```
世界观
我如何看待这个世界。
[世界观] [思考]
...在长期的运动过程中，一些反应被保留，而一些反应迅速消亡，
在整体上体现出一种定向选择，即为达尔文的生物进化论，优胜劣汰，
**自然选择**。从一些简单条件反射，再到一些复杂的条件反射...
```

**实现**：

修改 `SearchClient.tsx`：
- 对每条匹配结果，在 `content` 正文中找到搜索词第一次出现的位置
- 截取该位置**前后各 30 个字符**作为上下文
- 如果匹配在标题或描述中，取正文开头 80 字作为片段
- 片段中搜索词用 `<mark>` 标签高亮
- 结果卡片新增一行灰色小字显示片段

**改动范围**：
- 仅 `SearchClient.tsx` 一个文件，不影响其他任何功能
- `getAllContent()` 已返回正文内容，无需后端改动

### 待定

- 匹配多个关键词时显示第一个还是最相关的片段？
- 标题/描述已匹配时是否还要显示正文片段？

---

## 三、已解决：作品栏目中文路径 dev 模式报错

**问题**：`content/works/` 下用中文命名子目录（"文稿"、"设计"），`npm run dev` 访问嵌套页面时报 500：
> Page "/works/[...path]/page" is missing param "/works/[...path]" in "generateStaticParams()"

**原因**：Next.js 16.2.6 (Turbopack) dev 模式下，`generateStaticParams` 对含中文的 catch-all 路由参数匹配失败。英文路径正常。`npm run build` 和生产环境无此问题。

**方案**：子目录统一用英文命名。URL 更干净（`/works/design/` vs `/works/%E8%AE%BE%E8%AE%A1/`），显示可通过 frontmatter `title` 字段保持中文。

**实际执行**：`文稿` → `writing`，`设计` → `design`。dev 模式全线 200。

---

## 四、最近完成

- **作品栏目文件夹式分类**：`works/[...path]` catch-all 路由，子目录 = 系列，支持任意深度
- **共创栏目上线**：读者 Markdown 投稿 + 审核 + 评论，首页卡片 + 导航入口
- **三观内容架构完善**：世界观/人生观/价值观 + 爱情观
- **2026-08-17 安全与部署修复**：
  - 管理接口鉴权中间件路径修正，`/api/admin/*` 必须携带 `x-admin-key`
  - `ADMIN_KEY` 改为必填环境变量，未配置时服务拒绝启动（`server/.env.example` 提供模板）
  - 数据库落盘改为“临时文件 + rename”原子写，并修复并发初始化问题
  - Dockerfile 改为多阶段构建，不再依赖手工 scp dist；compose 端口只绑定 127.0.0.1
  - `upload.js` 修复部分上传失败后 manifest 误标为已上传的问题，并加入自动重试
  - 新增 `upload-quant.js`，量化仪表盘可一键同步到 COS 的 `works/quant/` 前缀
  - 前端统一 `API_BASE`，修复表单网络异常卡死、社区详情 404、works 评论 slug 冲突
  - 详细记录见 `docs/本次代码审查与修改报告_2026-08-17.md`

---

## 五、运行状态

| 组件 | 状态 |
|------|------|
| 本地开发 | ✅ localhost:3099 |
| 后端 API | ✅ api.xolnxoln.cn |
| 网站 xolnxoln.cn | ✅ |
| 网站 www.xolnxoln.cn | ✅ |
| 留言板 | ✅ /guestbook |
| 共创栏目 | ✅ /community（含评论） |
| ICP 备案 | ✅ 湘ICP备2026019191号 |
| 公安备案 | ✅ 已完成 |
| SSH/SCP | ❌ 网络层阻断（用 WebShell 替代） |

---

## 六、备案情况

| 事项 | 状态 |
|------|------|
| 管局 ICP 备案 | ✅ 湘ICP备2026019191号 |
| ICP 备案号悬挂 | ✅ 页脚已添加 |
| 公安联网备案 | ✅ 已完成 |

---

## 七、本地开发注意事项

- 启动命令：`npm run dev -- --port 3099`（不是 `npm run dev`，默认是 3000）
- 打不开时：[docs/开发审查清单.md](docs/开发审查清单.md) 第五节速查
